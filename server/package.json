{
  "name": "productive-content-backend",
  "version": "1.0.0",
  "description": "Backend API for productive content consumption app",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js --ignore logs/",
    "prod": "NODE_ENV=production node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --ext .js",
    "health": "curl -f http://localhost:5000/api/health || exit 1",
    "logs": "tail -f logs/combined.log",
    "clean-logs": "rm -rf logs/*.log"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "node-fetch": "^3.3.2",
    "express-validator": "^7.0.1",
    "compression": "^1.7.4",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "keywords": ["express", "mongodb", "api", "content", "learning"],
  "author": "",
  "license": "ISC"
}